# Recipe 30: Building an MCP Server in TypeScript

You need a custom MCP server and want to implement it in TypeScript, but you're not sure how to use the SDK or structure the project.

## Files

- `package.json` — Node.js package configuration with MCP SDK dependency
- `tsconfig.json` — TypeScript compiler configuration
- `src/index.ts` — Full Todo MCP server implementation with three tools (add, list, complete)

## Book Reference

See Chapter 6 of *Claude Code Cookbook* for the full recipe.
