{
  "name": "get_order_history",
  "description": "Returns order history for a user ID, sorted by date descending. Returns up to 100 records; use the cursor parameter to paginate. Returns an empty array if the user ID doesn't exist.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "user_id": {
        "type": "string",
        "description": "Unique user identifier (e.g., usr_abc123)"
      },
      "limit": {
        "type": "integer",
        "description": "Max records to return (1-100, default: 20)",
        "minimum": 1,
        "maximum": 100,
        "default": 20
      }
    },
    "required": ["user_id"]
  }
}
